# Elder Scrolls: Автоприключения ⚔️

Авто-RPG игра в мире The Elder Scrolls с абсурдным юмором в стиле Godville. Ваш герой автоматически переживает невероятные приключения в мире, где драконы пекут кексы, носки телепортируются, а грибы становятся королями.

## 🎮 Особенности

- **Автоматические события**: Герой живёт своей жизнью, генерируя события каждые 10-30 секунд
- **50+ абсурдных событий**: Битвы с кексами-драконами, философские беседы с лошадьми, дождь из сыров
- **40+ уникальных предметов**: Носки-телепорты, медовые шлемы, сахарные бомбы и многое другое
- **100+ мыслей героя**: Абсурдные размышления вашего персонажа
- **Арена PvP**: Сражайтесь с героями других игроков с абсурдными комментариями
- **Встречи героев**: Случайные взаимодействия между героями разных игроков
- **Мини-игры**: Угадайте мысль дракона и получите награды
- **Idle система**: Специальные события, когда игрок не активен
- **Real-time обновления**: Server-Sent Events для мгновенного получения событий

## 🛠 Технологии

- **Frontend**: SvelteKit + TypeScript
- **Styling**: Tailwind CSS + SkeletonUI (кастомная vintage-фэнтези тема)
- **Database**: PostgreSQL + DrizzleORM
- **Cache**: Redis (для очередей событий и кэширования)
- **Authentication**: JWT + bcrypt

## 📦 Установка

### Требования

- Node.js 18+
- PostgreSQL 14+
- Redis 7+

### Шаги установки

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd eldersgod
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

4. Отредактируйте `.env` и укажите свои настройки:
```env
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/eldersgod
REDIS_URL=redis://localhost:6379
JWT_SECRET=your-super-secret-jwt-key
PUBLIC_APP_URL=http://localhost:5173
```

5. Создайте базу данных:
```bash
createdb eldersgod
```

6. Сгенерируйте и примените миграции:
```bash
npm run db:generate
npm run db:push
```

7. Заполните базу начальными данными:
```bash
npm run db:seed
```

8. Запустите сервер разработки:
```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:5173

## 🐳 Docker (рекомендуется)

Используйте Docker Compose для простого запуска всех сервисов:

```bash
docker-compose up -d
```

Это запустит:
- PostgreSQL на порту 5432
- Redis на порту 6379
- Приложение на порту 5173

## 📚 Структура проекта

```
src/
├── lib/
│   ├── server/           # Серверная логика
│   │   ├── db/           # Схема БД и подключение
│   │   ├── auth.ts       # Аутентификация
│   │   ├── redis.ts      # Redis утилиты
│   │   ├── eventQueue.ts # Обработка очереди событий
│   │   └── seed.ts       # Seed данные
│   ├── game/             # Игровая логика
│   │   ├── eventGenerator.ts  # Генератор событий
│   │   ├── progression.ts     # Система прогрессии
│   │   ├── arena.ts           # Система арены
│   │   ├── encounters.ts      # Встречи героев
│   │   ├── idleDetector.ts    # Idle система
│   │   └── data/              # Игровой контент
│   │       ├── creatures.ts   # Существа
│   │       ├── items.ts       # Предметы
│   │       ├── actions.ts     # Действия
│   │       ├── locations.ts   # Локации
│   │       ├── titles.ts      # Титулы
│   │       └── thoughts.ts    # Мысли героя
│   ├── components/       # UI компоненты
│   │   ├── EventCard.svelte
│   │   ├── HeroPanel.svelte
│   │   ├── EventScroll.svelte
│   │   ├── ArenaPanel.svelte
│   │   └── minigames/    # Мини-игры
│   └── stores/           # Svelte stores
│       └── gameStore.ts
├── routes/               # Страницы и API
│   ├── +page.svelte      # Лендинг
│   ├── auth/             # Аутентификация
│   ├── game/             # Игровые страницы
│   └── api/              # API endpoints
└── app.css               # Глобальные стили
```

## 🎨 Дизайн

Игра выполнена в vintage-фэнтези стиле с:
- Тёмно-фиолетовым фоном (#2A1A3D)
- Золотыми акцентами (#D4AF37)
- Текстурами пергамента
- Анимациями (glow, float, particles, scroll unroll)
- Responsive дизайном для мобильных устройств

## 🎯 Как играть

1. **Регистрация**: Создайте аккаунт и героя
2. **Наблюдение**: Ваш герой автоматически переживает приключения
3. **Интерактивность**: Некоторые события имеют кнопку "Кликни!" - нажимайте для особых эффектов
4. **Мысли героя**: Кликайте на мысли для запуска мини-игр
5. **Арена**: Встаньте в очередь арены для PvP боёв
6. **Встречи**: Ваш герой может случайно встретить других героев

## 🔧 NPM Scripts

- `npm run dev` - Запуск dev сервера
- `npm run build` - Сборка для production
- `npm run preview` - Предпросмотр production сборки
- `npm run db:generate` - Генерация миграций
- `npm run db:push` - Применение миграций
- `npm run db:studio` - Открыть Drizzle Studio
- `npm run db:seed` - Заполнение БД начальными данными

## 🌍 Production Deployment

1. Убедитесь, что все env переменные установлены
2. Соберите приложение: `npm run build`
3. Запустите production сервер: `node build`

Рекомендуется использовать:
- PM2 для управления процессом
- Nginx как reverse proxy
- PostgreSQL и Redis на отдельных серверах

## 📝 Лицензия

MIT

## 🤝 Вклад

Pull requests приветствуются! Для больших изменений сначала откройте issue.

## 🎭 Особенности игрового мира

### Существа
- Кекс-дракон, дракон-пчеловод, дракон-философ
- Грибы-воины, говорящие грибы, королевские грибы
- Лошади-философы, коты-требующие-рыбку, медведи-поэты
- Ковры-торговцы, лестницы с характером, говорящие камни
- Сырные големы, летающие сыры, короли сыров

### Предметы
- Носок-телепорт, Меч из замороженного крика, Ложка судьбы
- Медовый шлем, Щит полуночной сырности, Сапоги вечного танца
- Кольцо говорящих облаков, Сахарная бомба, Золотой зуб удачи
- Свиток "Фус-Ро-Лё", Портрет облака, Джемная душа

### Локации
- Вайтран (город, где драконы на диете)
- Рифтен (город воров, крадущих только носки)
- Колодец с драконом, Пещера говорящих камней
- Таверна "У мёртвого барда", Лес танцующих папоротников
- Гора вечного сыра, Долина философских лошадей
- Королевство кексов, Город грибов (они правят)

---

**О Великий! Приключение ждёт!** 🐉
